{
  "name": "cardstock",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Live HTML cards as physical objects — a web experiment.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/matter-js": "^0.19.8",
    "typescript": "^5.6.3",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "matter-js": "^0.20.0"
  }
}
